/*
 * configuration of module
 */

/* should be a absolute path */
let path;

/* should be a hash function like md5 */
let hash;

let fm = module.exports = function fm(config){
    if(config){
        path = config.path;
        hash = config.hash;
    }

    return fm;
};

fm.store = function(file){
}

fm.getPath = function(fileName){
}

fm.delete = function(fileName){
}
